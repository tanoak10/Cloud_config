spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tanoak10/cloud-config.git
          username: tanoak10
          password: ks970528
server:
  port: 1201
